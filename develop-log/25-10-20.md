## lyingloong-25-10-20

a try to use bitsandbytes to quantize

tools for make
```
conda install gcc_linux-64 gxx_linux-64
```

```
git clone git@github.com:ROCm/bitsandbytes.git
cd bitsandbytes
git checkout rocm_enabled

export COMPUTE_BACKEND=hip
export BNB_ROCM_ARCH=gfx1100
export PATH=/usr/bin:$PATH

cmake -DCOMPUTE_BACKEND=hip -DBNB_ROCM_ARCH="gfx1100" -S .
make -j$(nproc)

python -m pip install .
```

test_bnb
```
import os
os.environ["BNB_ROCM_VERSION"] = "63"


import bitsandbytes as bnb
print(bnb.__version__)
```
out:
```
(rocm_ai) wanghao@amdic:~/lyingloong$ python test_bnb.py 
0.48.0.dev0
```